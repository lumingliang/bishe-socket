#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../raft-logger-redis/bin/logster.js" "$@"
  ret=$?
else 
  node  "$basedir/../raft-logger-redis/bin/logster.js" "$@"
  ret=$?
fi
exit $ret
